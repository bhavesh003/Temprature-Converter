Great. Backend is compiling now.
Now we move to:
STEP 6 — FRONTEND EMAIL OTP INTEGRATION
We will now implement registration email verification UI in Angular.
FINAL REGISTRATION FLOW
Plain text
Enter details
      ↓
Click Send OTP
      ↓
OTP received on email
      ↓
Enter OTP
      ↓
Verify OTP
      ↓
Enable Register button
STEP 6.1 — OPEN auth.service.ts
Go to:
Plain text
client/src/app/services/auth.service.ts
ADD THESE METHODS
Paste BELOW existing methods:
TypeScript
sendEmailOtp(email: string) {

  return this.http.post(
    `${this.baseUrl}/auth/send-email-otp`,
    { email }
  );
}
ADD VERIFY METHOD
TypeScript
verifyEmailOtp(
  email: string,
  otp: string
) {

  return this.http.post(
    `${this.baseUrl}/auth/verify-email-otp`,
    {
      email,
      otp
    }
  );
}
STEP 6.2 — OPEN register.component.ts
Go to:
Plain text
client/src/app/auth/register/register.component.ts
ADD THESE VARIABLES INSIDE CLASS
TypeScript
otpSent = false;

otpVerified = false;

emailOtp = '';
STEP 6.3 — ADD sendOtp() METHOD
Paste inside class:
TypeScript
sendOtp() {

  const email =
    this.registerForm.value.email;

  this.authService
      .sendEmailOtp(email)
      .subscribe({

        next: () => {

          this.otpSent = true;

          alert(
            'OTP sent successfully'
          );
        },

        error: () => {

          alert(
            'Failed to send OTP'
          );
        }
      });
}
STEP 6.4 — ADD verifyOtp() METHOD
Paste BELOW previous method:
TypeScript
verifyOtp() {

  const email =
    this.registerForm.value.email;

  this.authService
      .verifyEmailOtp(
          email,
          this.emailOtp
      )
      .subscribe({

        next: () => {

          this.otpVerified = true;

          alert(
            'OTP verified successfully'
          );
        },

        error: () => {

          alert(
            'Invalid OTP'
          );
        }
      });
}
STEP 6.5 — PREVENT REGISTER BEFORE OTP VERIFICATION
Inside your existing register method:
ADD THIS AT TOP:
TypeScript
if (!this.otpVerified) {

  alert(
    'Please verify email OTP first'
  );

  return;
}
STEP 6.6 — OPEN register.component.html
Go to:
Plain text
client/src/app/auth/register/register.component.html
ADD SEND OTP BUTTON BELOW EMAIL FIELD
Paste:
HTML
<button
  type="button"
  class="btn btn-warning mt-2"
  (click)="sendOtp()">

  Send OTP
</button>
ADD OTP INPUT FIELD
Paste BELOW Send OTP button:
HTML
<div *ngIf="otpSent">

  <input
    type="text"
    class="form-control mt-2"
    placeholder="Enter OTP"
    [(ngModel)]="emailOtp"
    name="emailOtp">

</div>
ADD VERIFY OTP BUTTON
Paste BELOW OTP input:
HTML
<div *ngIf="otpSent">

  <button
    type="button"
    class="btn btn-success mt-2"
    (click)="verifyOtp()">

    Verify OTP
  </button>

</div>
ADD SUCCESS MESSAGE
Paste BELOW verify button:
HTML
<div
  *ngIf="otpVerified"
  class="text-success mt-2">

  Email verified successfully
</div>
STEP 6.7 — DISABLE REGISTER BUTTON
Find your Register button.
Modify:
HTML
[disabled]="!otpVerified"
Example:
HTML
<button
  type="submit"
  [disabled]="!otpVerified">

  Register
</button>
STEP 6.8 — IMPORT FormsModule
If ngModel gives error:
Open:
Plain text
app.module.ts
Ensure:
TypeScript
import { FormsModule } from '@angular/forms';
AND:
TypeScript
FormsModule
inside imports array.
AFTER THIS
Restart Angular:
Bash
ng serve
TEST COMPLETE FLOW
TEST 1
Click:
Plain text
Send OTP
Expected:
✅ Email received
TEST 2
Enter OTP.
Click:
Plain text
Verify OTP
Expected:
✅ OTP verified
TEST 3
Register button enabled.
TEST 4
Register successfully.
AFTER COMPLETING
Say:
Plain text
Step 6 completed
Then we’ll implement:
STEP 7 — Forgot Password using Email OTP
